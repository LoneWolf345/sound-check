# ===========================================
# Sound Check - Environment Variables
# ===========================================

# ┌─────────────────────────────────────────┐
# │   BUILD-TIME VARIABLES (VITE_*)         │
# └─────────────────────────────────────────┘
# These variables are embedded into the JavaScript bundle at build time.
# Pass them as --build-arg when running docker build.

# Supabase Configuration (required)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here
VITE_SUPABASE_PROJECT_ID=your-project-id

# ┌─────────────────────────────────────────┐
# │   BUILD-TIME VARIABLES - DEV ONLY       │
# └─────────────────────────────────────────┘
# These are ONLY used in development mode to call internal APIs directly.
# In production, requests go through the proxy instead.

# Billing API - Account validation (optional, for local dev)
# VITE_BILLING_API_URL=https://acp-middleware-account-billing-system-prod.apps.prod-ocp4.corp.cableone.net

# CM Info API - Device lookup via SpreeDB (optional, for local dev)
# VITE_CM_INFO_API_URL=http://phoenix.polling.corp.cableone.net:4402

# ┌─────────────────────────────────────────┐
# │   RUNTIME VARIABLES (Server-Side)       │
# └─────────────────────────────────────────┘
# These variables are used by the Vite preview server at runtime.
# They are NOT embedded in the JavaScript bundle.
# Set via OpenShift ConfigMap, docker run -e, or export before starting.

# Billing API URL - PRODUCTION (required for account validation)
# The Vite preview middleware proxies /api/billing/* requests to this URL.
# Use internal cluster DNS for pod-to-pod communication in OpenShift:
BILLING_API_URL=http://acp-middleware-account.billing-system-prod.svc.cluster.local

# CM Info API URL - PRODUCTION (required for device lookup)
# The Vite preview middleware proxies /api/cm/* requests to this URL.
# Example: http://phoenix.polling.corp.cableone.net:4402
CM_INFO_API_URL=http://phoenix.polling.corp.cableone.net:4402
