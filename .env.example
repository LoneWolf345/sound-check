# ===========================================
# Sound Check - Build-Time Environment Variables
# ===========================================
# These variables are embedded into the JavaScript bundle at build time.
# Pass them as --build-arg when running docker build.

# Supabase Configuration (required)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here
VITE_SUPABASE_PROJECT_ID=your-project-id

# Poller Service URL (optional)
# If set, enables real account validation via the billing API
# If not set, mock validation is used for development
# VITE_POLLER_SERVICE_URL=http://soundcheck-api.apps.prod-ocp4.corp.cableone.net
