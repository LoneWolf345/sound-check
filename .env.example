# ===========================================
# Sound Check - Environment Variables
# ===========================================

# ┌─────────────────────────────────────────┐
# │   BUILD-TIME VARIABLES (VITE_*)         │
# └─────────────────────────────────────────┘
# These variables are embedded into the JavaScript bundle at build time.
# Pass them as --build-arg when running docker build.

# Supabase Configuration (required)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here
VITE_SUPABASE_PROJECT_ID=your-project-id

# Poller Service URL - DEVELOPMENT ONLY (optional)
# Used for local development to call the poller service directly.
# In production, this is NOT used - requests go through the proxy instead.
# VITE_POLLER_SERVICE_URL=http://localhost:3001

# ┌─────────────────────────────────────────┐
# │   RUNTIME VARIABLES (Server-Side)       │
# └─────────────────────────────────────────┘
# These variables are used by the Vite preview server at runtime.
# They are NOT embedded in the JavaScript bundle.
# Set via OpenShift ConfigMap, docker run -e, or export before starting.

# Poller Service URL - PRODUCTION (required for API access)
# Internal Kubernetes service URL for the poller service.
# The Vite preview middleware proxies /api/poller/* requests to this URL.
# Example: http://soundcheck-poller.soundcheck.svc.cluster.local:3001
POLLER_API_URL=http://soundcheck-poller.soundcheck.svc.cluster.local:3001
